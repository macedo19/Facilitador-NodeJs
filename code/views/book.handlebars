<div id="book-page">
    {{!-- dados vindo do back pela rota e sendo utilizados no html --}}
    <a href="/books">Voltar</a>
    <div class="container">
        <h1>{{book.title}}</h1>
        <p>Este livro possui {{book.pageqty}} páginas</p>
        <div class="actions">
            <a href="/books/edit/{{book.id}}">Editar</a>
            <form action="/books/remove/{{book.id}}" method="POST">
                <input type="submit" value="Remove">
            </form>
        </div>
    </div>
</div>